[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "probabilistic-analytic-engine"
version = "0.6.0"
description = "Auditable evidence, human-reviewed probability calculation and domain analysis"
requires-python = ">=3.12"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.3"]

[project.scripts]
pae-assess = "pae.cli:main"
pae-domain = "pae.domains:main"
pae-probability-contract = "pae.probability_contract:main"
pae-calculate = "pae.calculation:main"

[tool.setuptools]
packages = ["pae"]

[tool.pytest.ini_options]
addopts = "-q"
testpaths = ["tests"]
