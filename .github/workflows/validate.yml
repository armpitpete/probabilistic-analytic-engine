name: Validate PAE foundation

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      - run: python -m pip install --upgrade pip
      - run: python -m pip install -e ".[dev]"
      - run: python -m compileall -q pae tests
      - run: pytest
      - run: python -m pae fixtures/ceuta-2026-method-fixture.json --output generated/ceuta-2026-method-assessment.json
      - run: test -s generated/ceuta-2026-method-assessment.json
